- Escoger el tipo de enhancement.

- Preprocessing then reduction or reduction then preprocessing? (if preprocessing can come after it will be better to run different experiemtns in the same database)
	Que tamaño real tienen las mamografias de BCDR
	Como leer las TIFFs con sus 14-bits

- Escoger si masas o microcalcifications

- Manual examination to delete people with implants or things like that.

- Crear el heatmap para las lesiones:
	Connect the positions which are open.
	Add one that includes all lesions and one that has only malign lessions per 		patient.
	Check some random examples to see if it works

- Crear el DB 
	Separate training, cv and, test
	Escoger como como guardar labels de cada patch.

- Install TensorFlow on machines at A3
